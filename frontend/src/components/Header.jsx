import React from 'react';

const Header = ({ onSidebarToggle, sidebarOpen }) => {
  return (
    <header style={{ background: '#222', color: '#fff', padding: '16px 32px', fontSize: '1.5rem' }}>
      Admin Header
    </header>
  );
};

export default Header; 