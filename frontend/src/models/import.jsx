class Import {
    constructor(idImport,importDate,idProvider,totalAmount) {
        this.idImport = idImport;
        this.importDate = importDate;
        this.idProvider = idProvider;
        this.totalAmount = totalAmount;
    }
}
export default Import;